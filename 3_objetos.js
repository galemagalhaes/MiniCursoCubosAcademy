//OBJETOS
//Objetos contém informações em pares de "chave: valor" e podem ser agrupados

const pessoa = {
    nome: "Glaucia",
    idade: 37,
    altura: 1.75,
    peso: 80
  };
    console.log(pessoa); //acessa todas as informações do objeto pessoa
  
  //para acessar apenas 1 das informações do objeto usa-se:
  //objeto.informação
  
  console.log(pessoa.idade); //acessa apenas a informação idade do objeto pessoa
  console.log(pessoa.nome); //acessa apenas a informação nome do objeto pesso
  console.log(pessoa.altura); //acessa apenas a informação altura do objeto pesso
  console.log(pessoa.peso); //acessa apenas a informação peso do objeto pesso
